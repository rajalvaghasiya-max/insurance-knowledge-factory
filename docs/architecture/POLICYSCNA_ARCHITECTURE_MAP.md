# PolicyScna Architecture Map — Historical

> **HISTORICAL / SUPERSEDED. DO NOT USE THIS FILE FOR CURRENT CAPABILITY OR ARCHITECTURE DECISIONS.**

The current human-readable architecture memory is generated deterministically at:

`docs/architecture/generated/POLICYSCNA_CAPABILITY_MAP.md`

Its semantic source of truth is `governance/capabilities/catalog.json` plus `governance/capabilities/catalog.d/`; its implementation binding is reconciled against `governance/capabilities/generated/structural_fingerprints.json` and CI.

The former C1 architecture-map content remains available in Git history, including its last historical baseline at commit `593a5d2afb0ee4c9cdc564941ef9ff4e1a6478e7`.
